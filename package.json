{
  "name": "meizi-logger",
  "version": "1.0.1",
  "description": "Can log \"hello world\" and \"goodbye world\" to the console!",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "lib": "npm run build && npm publish",
    "publish-beta": "npm run build && npm publish --tag beta",
    "publish-dryrun": "npm run build && npm publish --dry-run"
  },
  "keywords": [
    "log"
  ],
  "author": "Meizi <meizirain@foxamil.com>",
  "license": "MIT",
  "dependencies": {
    "typescript": "^4.2.3"
  }
}
